[package]
name = "threeterm-slvs-worker"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license = "GPL-3.0-or-later"
publish.workspace = true
description = "Disposable sketch-constraint solver worker (libslvs, GPLv3 — see NOTICE for upstream provenance, modifications, and build steps)."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[features]
default = []

[lints]
workspace = true