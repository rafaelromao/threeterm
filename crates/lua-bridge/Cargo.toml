[package]
name = "threeterm-lua-bridge"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Restricted Lua bridge for keymaps and registered-command automation."

[dependencies]
mlua = { version = "0.11", default-features = false, features = ["lua54", "vendored", "serialize"] }
serde = { workspace = true }
serde_json = { workspace = true }
threeterm-protocol = { path = "../protocol" }

[lints]
workspace = true
